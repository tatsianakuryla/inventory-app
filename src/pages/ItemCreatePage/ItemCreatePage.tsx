import { type JSX } from 'react';
import { PageHeader } from '../../components/PageHeader/PageHeader';

export const ItemCreatePage = (): JSX.Element => {
  return <PageHeader title="Create Item" />;
};
